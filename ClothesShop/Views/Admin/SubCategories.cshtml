@model IEnumerable<ClothesShop.Models.SubcategoryModel>

@{
    ViewBag.Title = "SubCategories";
}

<div id="loginContainer">
	@Html.Partial("AdminMenu")
</div>

<h2>SubCategories</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            SubcategoryName
        </th>
        <th>
            CategoryID
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SubcategoryName)
        </td>
        <td>
            @Html.ActionLink("Delete", "RemoveSubCategory", new { id = item.ID }, new { onclick = "return confirm(\"Are you sure want to delete this?\")" })
        </td>
    </tr>
}

</table>
@Html.ActionLink("Add new", "AddSubCategory")